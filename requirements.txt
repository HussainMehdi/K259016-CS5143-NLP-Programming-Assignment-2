# Core data + ML (CPU/GPU-agnostic packages)
numpy>=1.26.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0

# Notebook + viz
jupyterlab>=4.0.0
ipykernel>=6.25.0
ipywidgets>=8.0.0
matplotlib>=3.8.0
seaborn>=0.13.0
tqdm>=4.66.0
tabulate>=0.9.0

# NLP / DL helpers (optional but useful for transformer models)
transformers>=4.36.0
datasets>=2.16.0
accelerate>=0.26.0
sentencepiece>=0.1.99
safetensors>=0.4.0

# Install PyTorch with CUDA separately (https://pytorch.org/get-started/locally/) so the
# wheel matches your driver/CUDA stack. Do not pin torch here.
